# CLAUDE.md（nyan）

- スキルは3層（フロー／プロセス／ツール）。**依存は プロセス → フロー・ツール の一方向**。フローがツールを呼ぶ、ツールが業務の判断をする、プロセスが DB のスキーマを直に持つ、のいずれも禁止
- `skills/flow-donyu/references/` は donyu-standard の**写し**。手で直さない。直すなら donyu-standard に PR を出し、`scripts/sync-standard.sh` で取り込む。`VERSION` を更新する
- 外部システムへ**書く**スキル（`progress`・`notify`）は `disable-model-invocation: true`。明示的に呼ばれたときだけ動く
- Notion のマイルストーン DB は**本体がページ、プロパティは要約**。「現状」「ギャップと打ち手」「事実確認」「タスク」は最新の断面に置き換え、「レビュー」「更新履歴」「当てはめられなかった事実」は追記のみ
- 判断できる情報が無ければ判断しない。「事実確認」に置いて人に返す
- フロントはレビューし、直すのは nyan（`progress --review`）。人の書き込みは上書きしない
- Slack の投稿先・タスク表などの所在はリポジトリに書かない（案件ごとに渡す）
- 根拠 URL を付けられない○×は付けない。議事録に無いことは「未確認」であって「×」ではない
- Slack への投稿は必ず下書き → 承認 → 投稿。黙って投稿しない
- 顧客の PII を書かない（医療機関名は可）
- 評価のルールを変えるときは `skills/progress/SKILL.md` だけを直す。変えた理由を `CHANGELOG.md` に1行残す
